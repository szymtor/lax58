# Lax-58 workflow

Read `CURRENT_STATE.md` and `WORKFLOW.md` before work. Consult
`SESSION_HISTORY.md` only for relevant historical decisions.
Follow the current `lax print instructions` and `lax print spec`.
Keep concepts separate from proofs and honor their review boundary unless
the user has already authorized both phases. Never run `lax register`.
Preserve unrelated changes and `Archive.zip`.
Update current status, validation evidence, blockers, and exact next action
before ending a session; append historical detail to `SESSION_HISTORY.md`.
